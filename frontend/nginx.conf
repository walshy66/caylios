server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback: every subscriber subdomain serves the same app shell;
    # the backend resolves the workspace from the Host header.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
